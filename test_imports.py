print("Importing os...", flush=True)
import os
print("Importing sys...", flush=True)
import sys
print("Importing cv2...", flush=True)
import cv2
print("Importing numpy...", flush=True)
import numpy
print("Importing yaml...", flush=True)
import yaml
print("Importing ultralytics...", flush=True)
from ultralytics import YOLO
print("Importing psutil...", flush=True)
import psutil
print("Importing torch...", flush=True)
import torch
print("Importing requests...", flush=True)
import requests
print("Importing winsound...", flush=True)
import winsound
print("Importing win32com.client...", flush=True)
import win32com.client
print("All imports OK!", flush=True)
